# Release Notes

## Breaking Changes

- **Renamed `DirectoryJsonMetadataSettings` to `JsonFolderMetadataSettings`.**
  This is not backward compatible. Update all references from `DirectoryJsonMetadataSettings` to `JsonFolderMetadataSettings`.
