# Single source of truth for the library version (see docs/RELEASING.md).
# Local builds and nightlies are derived from this value; a stable release
# is only published manually via Actions -> Release.
VERSION=1.7.0
